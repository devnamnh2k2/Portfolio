import { ModeToggle } from '@/components/ui/mode-toggle';
import React from 'react';

const Footer = () => {
    return (
        <div>
            Footer working!
            <ModeToggle/>
        </div>
    );
};

export default Footer;